# Tipos de Variables - Ver .pdf
# Tipos de Operadores - Ver .pdf

nombre="Jorge"  # Le asignamos un valor a la variable.
type(nombre)  # Nos devuelve el tipo (class) de una variable. En este caso sería <class 'str'>. 
print(type(nombre))  # El tipo de variable lo define el contenido, no el contenedor.             
# Las CONSTANTES como tal no existen, y se definen en MAYÚSCULAS.

10 % 3 # Nos devuelve el resto de la división (Módulo).
15 // 4 # Nos devuelve el cociente entero (División Entera).
# Operador para asignar valores --->  =
# Operador para comparar valores --->  ==
